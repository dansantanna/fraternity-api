version: '3.4'

services:
  fraternity-api:
    image: fraternity-api
    volumes:
        - ./:/app
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 7001:7001

volumes:
  fraternity-api: